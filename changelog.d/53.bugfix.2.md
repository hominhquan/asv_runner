Releases publish the sdist alongside wheels again: the wheel and sdist jobs uploaded artifacts under the same default name and the publish step picked up only the wheel, which is why 0.2.5 reached PyPI wheel-only and downstream rebuilds from GitHub tarballs shipped wrong version metadata.
