`asv_runner.__version__` is now defined for source-tree and editable imports, resolving the installed distribution version with a `0.0.0.dev0` fallback; built artifacts keep the static build-time string.
